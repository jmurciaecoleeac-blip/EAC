// Font stack using locally available Liberation Sans (metric-compatible with Arial)
// Used as fallback when Google Fonts / network is unavailable in the render environment.
// Switch back to @remotion/google-fonts/Montserrat for online rendering.
export const FONT_FAMILY = "'Liberation Sans', Arial, Helvetica, sans-serif";
